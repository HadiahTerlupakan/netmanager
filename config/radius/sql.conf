# SQL Module Configuration for FreeRADIUS 3.x with PostgreSQL
#
# Reference: https://networkradius.com/doc/current/raddb/mods-available/sql.html
# Reference: https://freeradius.org/documentation/freeradius-server/3.2.9/tutorials/sql.html
#
# Placeholder tokens (__XXX__) are substituted at container startup
# by docker-entrypoint.sh with actual environment variable values.
#
sql {
    # Database driver - must match installed rlm_sql_* module
    driver = "rlm_sql_postgresql"

    # Database dialect - determines which queries.conf to use
    dialect = "postgresql"

    # Connection info (substituted at runtime by docker-entrypoint.sh)
    server = "__DB_HOST__"
    port = __DB_PORT__
    login = "__DB_USER__"
    password = "__DB_PASSWORD__"
    radius_db = "__DB_NAME__"

    # Connection pool settings
    pool {
        start = 5
        min = 4
        max = 20
        spare = 3
        uses = 0
        lifetime = 0
        idle_timeout = 60
        retry_delay = 1
        cleanup_interval = 30
    }

    # Read NAS clients from the database (nas table).
    # NAS entries are managed via the admin UI (/admin/network/mikrotik)
    # NOTE: Clients are ONLY read on server startup, not live.
    read_clients = yes

    # Table names (standard FreeRADIUS PostgreSQL schema)
    acct_table1 = "radacct"
    acct_table2 = "radacct"
    postauth_table = "radpostauth"
    authcheck_table = "radcheck"
    groupcheck_table = "radgroupcheck"
    authreply_table = "radreply"
    groupreply_table = "radgroupreply"
    usergroup_table = "radusergroup"

    # NAS table for read_clients
    client_table = "nas"

    # Group attribute specific to this instance of rlm_sql
    group_attribute = "SQL-Group"

    # Remove stale sessions if checkrad does not see a double login
    delete_stale_sessions = yes

    # Read group membership
    read_groups = yes

    # Include the default PostgreSQL queries
    # This loads authorize, accounting, and post-auth queries from:
    # /etc/freeradius/mods-config/sql/main/postgresql/queries.conf
    $INCLUDE ${modconfdir}/sql/main/${dialect}/queries.conf

    # ==========================================================================
    # MULTI-TENANT QUERY OVERRIDES
    # ==========================================================================
    # We filter all queries by tenantId, where tenantId is resolved from the 
    # NAS (Router) that sent the request. This ensures that users with the 
    # same name in different tenants do not conflict.
    # ==========================================================================

    # --- Authorization Queries ---
    
    authorize_check_query = "SELECT id, username, attribute, value, op \
      FROM ${authcheck_table} \
      WHERE username = '%{SQL-User-Name}' \
      AND \"tenantId\" = (SELECT \"tenantId\" FROM ${client_table} WHERE nasname = '%{NAS-IP-Address}' LIMIT 1) \
      ORDER BY id"

    authorize_reply_query = "SELECT id, username, attribute, value, op \
      FROM ${authreply_table} \
      WHERE username = '%{SQL-User-Name}' \
      AND \"tenantId\" = (SELECT \"tenantId\" FROM ${client_table} WHERE nasname = '%{NAS-IP-Address}' LIMIT 1) \
      ORDER BY id"

    group_membership_query = "SELECT groupname \
      FROM ${usergroup_table} \
      WHERE username = '%{SQL-User-Name}' \
      AND \"tenantId\" = (SELECT \"tenantId\" FROM ${client_table} WHERE nasname = '%{NAS-IP-Address}' LIMIT 1) \
      ORDER BY priority"

    authorize_group_check_query = "SELECT id, groupname, attribute, value, op \
      FROM ${groupcheck_table} \
      WHERE groupname = '%{${group_attribute}}' \
      AND \"tenantId\" = (SELECT \"tenantId\" FROM ${client_table} WHERE nasname = '%{NAS-IP-Address}' LIMIT 1) \
      ORDER BY id"

    authorize_group_reply_query = "SELECT id, groupname, attribute, value, op \
      FROM ${groupreply_table} \
      WHERE groupname = '%{${group_attribute}}' \
      AND \"tenantId\" = (SELECT \"tenantId\" FROM ${client_table} WHERE nasname = '%{NAS-IP-Address}' LIMIT 1) \
      ORDER BY id"

    # --- Accounting Queries ---
    
    accounting_start_query = "INSERT INTO ${acct_table1} \
      (acctsessionid, acctuniqueid, username, realm, nasipaddress, nasportid, nasporttype, \
       acctstarttime, acctupdatetime, acctstoptime, acctinterval, acctsessiontime, \
       acctauthentic, connectinfo_start, connectinfo_stop, acctinputoctets, acctoutputoctets, \
       calledstationid, callingstationid, acctterminatecause, servicetype, framedprotocol, \
       framedipaddress, \"tenantId\") \
      VALUES \
      ('%{Acct-Session-Id}', '%{Acct-Unique-Session-Id}', '%{SQL-User-Name}', '%{realm}', \
       '%{NAS-IP-Address}', '%{NAS-Port}', '%{NAS-Port-Type}', '%S', '%S', NULL, 0, 0, \
       '%{Acct-Authentic}', '%{Connect-Info}', '', 0, 0, '%{Called-Station-Id}', \
       '%{Calling-Station-Id}', '', '%{Service-Type}', '%{Framed-Protocol}', \
       '%{Framed-IP-Address}', (SELECT \"tenantId\" FROM ${client_table} WHERE nasname = '%{NAS-IP-Address}' LIMIT 1))"

    accounting_update_query = "UPDATE ${acct_table1} \
      SET acctupdatetime = '%S', acctinterval = %{Acct-Interim-Interval:-0}, \
          acctsessiontime = %{Acct-Session-Time:-0}, \
          acctinputoctets = (('%{Acct-Input-Gigawords:-0}'::bigint << 32) + '%{Acct-Input-Octets:-0}'::bigint), \
          acctoutputoctets = (('%{Acct-Output-Gigawords:-0}'::bigint << 32) + '%{Acct-Output-Octets:-0}'::bigint), \
          framedipaddress = '%{Framed-IP-Address}' \
      WHERE acctuniqueid = '%{Acct-Unique-Session-Id}'"

    accounting_stop_query = "UPDATE ${acct_table1} \
      SET acctstoptime = '%S', acctsessiontime = %{Acct-Session-Time:-0}, \
          acctinputoctets = (('%{Acct-Input-Gigawords:-0}'::bigint << 32) + '%{Acct-Input-Octets:-0}'::bigint), \
          acctoutputoctets = (('%{Acct-Output-Gigawords:-0}'::bigint << 32) + '%{Acct-Output-Octets:-0}'::bigint), \
          acctterminatecause = '%{Acct-Terminate-Cause}', \
          framedipaddress = '%{Framed-IP-Address}' \
      WHERE acctuniqueid = '%{Acct-Unique-Session-Id}'"
}
