import EndorsementListClient from "./EndorsementListClient";

export const metadata = {
  title: "Surat Pengesahan | NetManager",
};

export default function EndorsementPage() {
  return <EndorsementListClient />;
}
