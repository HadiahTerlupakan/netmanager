const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.customer.count(); const customers = await prisma.customer.findMany({ select: { id: true, name: true, active: true }, orderBy: { createdAt: 'desc' } }); console.log('Count:', count); console.log('Customers:', JSON.stringify(customers, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
