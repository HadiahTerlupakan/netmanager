import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const count = await prisma.pelanggan.count(); const customers = await prisma.pelanggan.findMany({ select: { id: true, nama: true, status: true }, orderBy: { createdAt: 'desc' } }); console.log('Count:', count); console.log('Customers:', JSON.stringify(customers, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
