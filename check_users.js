import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const userCount = await prisma.user.count(); const logs = await prisma.systemLog.findMany({ take: 10, orderBy: { createdAt: 'desc' }, select: { subject: true, action: true, details: true } }); console.log('User Count:', userCount); console.log('Recent Logs:', JSON.stringify(logs, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
