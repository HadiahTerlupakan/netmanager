{
  "instructions": "git worktree is ALLOWED with mandatory cleanup (see CLAUDE.md 'Worktree Policy'): create -> work -> commit -> push -> git worktree remove, then verify with git worktree list. NEVER create or switch branches in the main working directory without explicit user instruction."
}
