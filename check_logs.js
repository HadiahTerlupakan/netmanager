import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const count = await prisma.systemLog.count(); const logs = await prisma.systemLog.findMany({ where: { subject: 'Pelanggan' }, orderBy: { createdAt: 'desc' }, take: 5 }); const woCount = await prisma.workOrder.count(); console.log('SystemLog Count:', count); console.log('Recent Pelanggan Logs:', JSON.stringify(logs, null, 2)); console.log('WorkOrder Count:', woCount); } main().catch(console.error).finally(() => prisma.$disconnect());
